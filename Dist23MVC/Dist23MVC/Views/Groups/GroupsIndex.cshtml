@model IEnumerable<Dist23MVC.Models.GroupViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "GroupsCreate")</br>

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.group.GroupName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.group.GroupNumber)
        </th>
        <th>
            @Html.Label("GSR")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.group.GroupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.group.GroupNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.contact.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "GroupsEdit", new { id=item.pKey }) |
            @Html.ActionLink("Details", "GroupsDetails", new { id=item.pKey }) |
            @Html.ActionLink("Delete", "GroupsDelete", new { id=item.pKey })
        </td>
    </tr>
}

</table>
