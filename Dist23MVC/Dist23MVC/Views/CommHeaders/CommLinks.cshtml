@model IEnumerable<Dist23MVC.Models.CommLinks>

@{
    ViewBag.Title = ViewBag.CommitteeTitle;
}

<div class="newsArea">
    <table align="center">
        <tr>
            <td>
                <h1>@ViewBag.CommTitle</h1>
                <h2>@ViewBag.CommDetail</h2>
            </td>
        </tr>
        @if (Session["loginName"] != null)
        {
            <tr>
                <td>
                    <h2>Welcome back, @Session["loginName"].ToString()</h2>
                </td>
            </tr>
            <tr>
                <td>
                    @Html.ActionLink("Add New", "LinksCreate")
                </td>
            </tr>
        }

        @foreach (var item in Model)
        {
            <tr>
                <td class="newsItem">
                    <hr size="5" style="font-weight: bold; background-color: #F4D453; height: 5px;" />
                    <h2>@Html.DisplayFor(modelItem => item.LinkHeader)</h2>
                    <a href="@Html.DisplayFor(modelItem => item.LinkURL)" target="_blank"> @Html.DisplayFor(modelItem => item.LinkText)</a><br />
                    @if (Session["loginName"] != null)
                    {
                    <td>
                        @Html.ActionLink("Edit", "LinksEdit", new { id = item.pKey }) |
                        @Html.ActionLink("Delete", "LinksDelete", new { id = item.pKey })
                    </td>
                    }
                </tr>
        }
    </table>

    
